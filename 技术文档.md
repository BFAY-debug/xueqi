# 「学栖」校园学习社区平台 — 技术文档

> 版本：v3.5.3 | 最后更新：2026-04-23

## 项目概述

「学栖」是一个面向大学生的**校园学习社区平台**，融合虚拟自习室、实体占座、社区讨论、积分排行、好友私聊等核心功能。整体 UI 风格为**古典中国风 × 书院气质**，配合毛玻璃效果与水墨装饰。

**技术要求满足情况：**
- 多服务协同：3 个独立微服务 + API 网关
- 容器部署：Docker Compose 编排 7 个容器
- 用户管理：注册/登录/登出/信息修改 + JWT 双 Token + RBAC（超级管理员/管理员/用户三级）
- 数据持久化：MySQL 8.0 关系型数据库（utf8mb4）+ Redis 5.0 缓存（AOF 持久化）

**核心功能模块：**
- **书院**（虚拟自习室）— 实时聊天、番茄钟、学习计时、状态同步、沉浸模式
- **云占座**（实体座位预约）— 座位图、签到、违约惩罚、志愿任务
- **知识广场**（社区）— 帖子/评论、Markdown 编辑器（图片上传/拖拽/粘贴）、@提及自动补全、匿名、点赞/收藏、编辑提案
- **书海**（书籍推荐）— 评分/书评、用户推送、ISBN 查 OpenLibrary API
- **金榜**（排行榜）— 积分/学习时长/签到/周榜/月榜
- **好友系统** — 申请/接受/拉黑、私聊（仅好友）、实时通知
- **关注系统** — 关注/粉丝、个人主页 `/user/:id`
- **管理后台** — 内容审核、用户管理、座位管理、书籍管理、反馈管理、志愿者审核、审核日志

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + Element Plus + Pinia + Vue Router |
| 实时通信 | Socket.IO（聊天、状态同步、座位更新、在线状态、好友事件） |
| 动画 | GSAP + ScrollTrigger（首页长翻页、滚动渐入） |
| 后端 | Node.js + Express（3 个微服务 + API 网关） |
| 共享模块 | `xueqi-shared`（DB 连接池、Redis、JWT、日志、错误处理） |
| 数据库 | MySQL 8.0（utf8mb4，20 连接池） |
| 缓存 | Redis 5.0（ioredis，AOF 持久化） |
| 认证 | JWT + bcrypt（accessToken 2h + refreshToken 7d） |
| 文件上传 | Multer（头像/封面/文章图片/聊天图片） |
| Markdown | marked + DOMPurify（渲染 + 安全过滤 + 自定义渲染器） |
| 部署 | Docker Compose（7 容器）+ Nginx 反向代理 |

---

## 系统架构

```
浏览器 ──→ Nginx(:80) ──→ API网关(:3000) ──┬──→ 用户服务(:3001)
         │                                   ├──→ 学习内容服务(:3002) ← Socket.IO
         │                                   └──→ 社区服务(:3003)
         │                                          │
         │                            ┌─────────────┴─────────────┐
         │                            │     MySQL 8.0(:3306)      │
         │                            │     Redis 5.0(:6379)      │
         │                            └───────────────────────────┘
         │
         ├── /socket.io/* ──→ 学习内容服务 (WebSocket, 24h 超时)
         ├── /uploads/*   ──→ 共享卷静态文件 (7 天缓存)
         └── /*           ──→ Vue SPA (index.html, 不缓存)
```

### 服务职责

| 服务 | 端口 | 职责 |
|------|------|------|
| **Nginx** | 80 | 静态资源、反向代理、WebSocket 转发、上传文件服务、限流 30r/s |
| **API 网关** | 3000 | 路由转发、全局限流 500req/15min、认证限流 50req/15min |
| **用户服务** | 3001 | 注册/登录/JWT/RBAC、用户信息、积分/等级/排行榜、通知、好友/关注/拉黑、头像上传 |
| **学习内容服务** | 3002 | 虚拟自习室、实体占座、书籍推荐/审核、番茄钟、学习记录、Socket.IO 服务端、私聊 |
| **社区服务** | 3003 | 帖子/评论、点赞/收藏、标签、@提及解析与通知、匿名、审核管理、编辑提案、文章图片上传 |

---

## 项目目录结构

```
D:\web\
├── docker-compose.yml          # 容器编排 (7 容器)
├── .env                        # 环境变量
├── 技术文档.md                  # 本文档
├── database/
│   └── init/
│       ├── 01-schema.sql       # 核心表结构
│       ├── 02-seed.sql         # 种子数据 (角色/等级/用户/自习室/座位/标签)
│       ├── 03-indexes.sql      # 复合索引
│       ├── 04-private-chat.sql # 私聊表
│       ├── 06-friend-system.sql # 好友系统表
│       ├── 07-user-follows.sql # 关注系统表
│       └── 08-account-id.sql   # account_id 字段
├── services/
│   ├── shared/                 # xueqi-shared 共享模块
│   │   └── src/
│   │       ├── index.js        # 统一导出
│   │       ├── db.js           # MySQL2 连接池 (20 connections, utf8mb4)
│   │       ├── redis.js        # ioredis (retry 策略, max 5s backoff)
│   │       ├── jwt.js          # generateToken / generateRefreshToken / verifyToken
│   │       ├── auth.js         # authMiddleware / optionalAuth / requireRole / serviceAuthMiddleware
│   │       ├── logger.js       # Winston JSON logger (colorize + timestamp)
│   │       ├── responseHandler.js  # res.success() / res.error() / res.paginate()
│   │       └── errorHandler.js # MySQL 重复 / JWT 错误 / Multer 大小限制 / 通用 500
│   ├── api-gateway/
│   │   └── src/app.js          # http-proxy-middleware 反向代理 + express-rate-limit
│   ├── user-service/
│   │   └── src/
│   │       ├── app.js
│   │       ├── routes/         # auth, profile, upload, points, notifications, friends, follows, admin, feedback
│   │       └── controllers/    # 各路由对应控制器
│   ├── study-service/
│   │   └── src/
│   │       ├── app.js          # Socket.IO 服务端 + Redis Pub/Sub 订阅
│   │       ├── routes/         # rooms, sessions, locations, seats, volunteer, chat, privateChat
│   │       ├── controllers/
│   │       └── socket/         # Socket.IO 事件处理器
│   └── community-service/
│       └── src/
│           ├── app.js
│           ├── routes/         # posts, comments, tags, proposals, bookmarks, admin, upload
│           ├── controllers/
│           └── services/       # mentionService (@提及解析、通知)
└── frontend/
    ├── Dockerfile              # 多阶段构建 → Nginx 静态服务
    ├── nginx.conf              # 反向代理 + SPA fallback + WebSocket
    └── src/
        ├── views/              # 16 个页面组件
        ├── components/         # 9 个可复用组件
        │   ├── AppNavbar.vue   # 全局导航栏 (毛玻璃, 通知铃铛, 积分条)
        │   ├── AppFooter.vue   # 全局页脚 (云纹装饰)
        │   ├── MarkdownEditor.vue  # Markdown 编辑器 (图片上传/拖拽/粘贴, @提及, 表格按钮)
        │   ├── MarkdownViewer.vue  # Markdown 渲染 (@链接, 图片拖拽缩放)
        │   ├── UserAvatar.vue  # 头像组件 (可点击弹资料卡)
        │   ├── UserProfileCard.vue # 用户资料卡弹窗 (加好友/发消息/拉黑)
        │   ├── BackButton.vue  # 返回按钮
        │   ├── AppLoading.vue  # 加载骨架屏
        │   └── AppEmpty.vue    # 空状态
        ├── stores/             # 4 个 Pinia Store
        │   ├── user.js         # 认证/用户信息/未读通知
        │   ├── chat.js         # 自习室聊天
        │   ├── friend.js       # 好友系统
        │   └── message.js      # 私聊消息
        ├── api/                # 7 个 API 模块
        │   ├── request.js      # Axios 基础配置 (拦截器, 自动 Token 刷新)
        │   ├── user.js         # 用户/积分/排行/通知/管理/反馈
        │   ├── study.js        # 自习室/座位/书籍/志愿者/聊天
        │   ├── community.js    # 帖子/评论/标签/提案/收藏/审核
        │   ├── message.js      # 私聊会话/消息
        │   ├── friend.js       # 好友/拉黑/搜索
        │   └── follow.js       # 关注/粉丝
        ├── router/index.js     # 18 条路由 + 导航守卫
        ├── composables/        # 3 个组合式函数
        │   ├── useTimeAgo.js   # 中文相对时间 (刚刚/X分钟前/X小时前/X天前)
        │   ├── useInkRipple.js # 水墨点击涟漪动画
        │   └── useSocket.js    # Socket.IO 单例连接 (心跳/自动重连)
        └── assets/
            ├── styles/
            │   ├── variables.css  # CSS 变量 (主题色/字体/间距/等级色)
            │   └── global.css     # 全局样式 (重置/毛玻璃/按钮/印章/Element Plus 覆写)
            ├── textures/       # 宣纸纹理 rice-paper.svg, 水墨素材 ink-dots.svg
            └── icons/          # 古典风格图标
```

---

## 数据库设计

**数据库名**: `xueqi_db` | **字符集**: `utf8mb4` | **排序规则**: `utf8mb4_unicode_ci`

### 完整表清单（34 张表）

#### 用户与权限

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `roles` | 三级 RBAC | id, name(super_admin/admin/user) |
| `levels` | 6 级等级 | id, name(书童~大儒), min_points, badge |
| `users` | 用户账户 | id, username, account_id(唯一), email, password_hash, nickname, avatar_url, bio, security_question, security_answer, role_id(FK), status(1=正常/0=禁言) |
| `user_stats` | 学习统计 | user_id(FK), total_study_minutes, total_pomodoros, total_points, level_id(FK), daily_points, daily_reset_date, penalty_count, penalty_reset_date, ban_until, follower_count, following_count, checkin_streak |
| `points_log` | 积分流水 | user_id, action(study/pomodoro/book_rate/post/comment/volunteer), points(+/-), description |
| `notifications` | 通知 | user_id, type(review_result/like/points/level_up/system/friend_request/mention), title, content, is_read, related_id, related_type |
| `admin_applications` | 管理员申请 | user_id, reason, status(pending/approved/rejected), reviewer_id |

#### 好友与社交

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `friend_requests` | 好友申请 | sender_id, receiver_id, message, status(pending/accepted/rejected) |
| `friendships` | 好友关系 | user1_id, user2_id (user1 < user2) |
| `blocks` | 黑名单 | blocker_id, blocked_id |
| `user_follows` | 关注关系 | follower_id + following_id (联合主键) |

#### 自习室与占座

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `study_rooms` | 虚拟自习室 | name, description, capacity, type(virtual/real), cover_image, status |
| `room_participants` | 参与者 | room_id, user_id, seat_number, is_studying |
| `study_sessions` | 学习记录 | user_id, room_id, session_type(pomodoro/free/timed), duration_minutes, status(active/completed/abandoned) |
| `room_messages` | 聊天消息 | room_id, user_id(nullable=系统), content, image_url, type(user/system/anonymous) |
| `message_reads` | 已读回执 | message_id + user_id (联合主键) |
| `real_locations` | 实体地点 | name, building, floor, open_time, close_time, total_seats |
| `real_seats` | 实体座位 | location_id(FK), seat_code, row_num, col_num, has_power, status(available/occupied/reserved/maintenance) |
| `seat_reservations` | 座位预约 | user_id, seat_id, reserve_date, start_time, end_time, status(pending/checked_in/completed/cancelled/no_show) |
| `volunteer_tasks` | 志愿任务 | name, description, reward_penalty, reward_points |
| `volunteer_records` | 志愿记录 | user_id, task_id, status(pending/confirmed/rejected), admin_id |

#### 社区

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `posts` | 帖子 | user_id, title, content(MEDIUMTEXT), summary, category(experience/question/resource/general), is_anonymous, content_type(markdown), version, is_pinned, is_featured, permission(public/private), status(pending/published/hidden) |
| `comments` | 评论 | post_id, user_id, parent_id(嵌套), content, is_anonymous, like_count, status |
| `tags` | 标签 | name(UNIQUE) |
| `post_tags` | 帖子标签 | post_id + tag_id (联合主键) |
| `post_likes` | 帖子点赞 | user_id + post_id (联合主键) |
| `comment_likes` | 评论点赞 | user_id + comment_id (联合主键) |
| `post_versions` | 版本历史 | post_id, version(int), title, content, edit_summary |
| `edit_proposals` | 编辑提案 | post_id, proposer_id, content, base_version, status(open/merged/rejected/closed) |
| `post_bookmarks` | 收藏 | user_id + post_id (联合主键) |
| `review_logs` | 审核日志 | reviewer_id, target_type(post/comment), action(approve/reject), reason |

#### 私聊

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `conversations` | 会话 | user1_id, user2_id(user1<user2), last_message_at |
| `private_messages` | 私聊消息 | conversation_id(FK), sender_id, content, image_url, is_read |

#### 反馈

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `feedback` | 用户反馈 | user_id, type, content, status(pending/resolved/ignored), admin_reply |

### 种子数据

- 3 个角色: super_admin(id=1), admin(id=2), user(id=3)
- 6 个等级: 书童(0), 秀才(100), 举人(500), 进士(1500), 翰林(4000), 大儒(10000)
- 5 个示例用户（密码: `password`）
- 5 个虚拟自习室（静修斋、明理堂、夜读轩、专攻阁、闲读居）
- 4 个实体地点（藏经阁A座/B座、讲经堂3号、明德书房），56 个物理座位
- 5 个志愿任务、15 个标签、5 篇示例帖子

---

## 认证与权限

### JWT 双 Token 机制

```
登录 → 返回 accessToken(2h) + refreshToken(7d)
     → 前端 localStorage 存储
     → Axios 拦截器自动附加 Authorization: Bearer <accessToken>
     → 401 响应时自动用 refreshToken 刷新（并发去重）
     → 刷新失败则跳转 /login?redirect=原路径
```

### 三级 RBAC

| 角色 | role_id | 权限范围 |
|------|---------|---------|
| **super_admin** | 1 | 全部权限 + 指定/取消管理员 + 系统统计/趋势 + 角色管理 + 反馈管理 |
| **admin** | 2 | 审核内容 + 管理座位/教室/书籍/自习室 + 禁言用户 + 志愿审核 |
| **user** | 3 | 正常使用所有功能、推送书籍（待审核）、发帖/评论、申请管理员 |

### 认证中间件

| 中间件 | 说明 |
|--------|------|
| `authMiddleware` | JWT 验证 + DB 用户查询 → `req.user` |
| `optionalAuth` | Token 存在时附加用户，不拦截未认证请求 |
| `requireRole(...roles)` | RBAC 工厂函数，检查 `req.user.roleName` |
| `serviceAuthMiddleware` | 服务间调用，要求 JWT 中 `type=service` |

### 内容审核策略

帖子/评论**直接发布**为 `published`，管理员后续管理（隐藏/编辑/删除/置顶/精选）。书籍推送需管理员审核。

---

## API 参考

### 网关路由

| 前缀 | 目标服务 | 特殊配置 |
|------|---------|---------|
| `/api/user/*` | user-service:3001 | `/register` `/login` 限流 50req/15min |
| `/api/study/*` | study-service:3002 | `ws: true` WebSocket 代理 |
| `/api/community/*` | community-service:3003 | — |

### 用户服务 `/api/user` (3001)

#### 认证
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/register` | 无 | 注册 |
| POST | `/login` | 无 | 登录 |
| POST | `/refresh` | 无 | 刷新 Token |
| POST | `/logout` | 需要 | 登出 |

#### 用户信息
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/profile` | 需要 | 自己的资料 |
| PUT | `/profile` | 需要 | 编辑资料 |
| GET | `/:id` | 可选 | 他人公开资料 |
| GET | `/:id/stats` | 可选 | 学习统计 |
| POST | `/upload/avatar` | 需要 | 上传头像（前端压缩 300×300） |

#### 积分与排行
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/points` | 需要 | 积分和等级 |
| GET | `/points/log` | 需要 | 积分流水 |
| POST | `/points/daily-checkin` | 需要 | 每日签到 |
| POST | `/points/award` | 服务间 | 内部积分发放 |
| GET | `/leaderboard` | 可选 | 积分总排行 |
| GET | `/leaderboard/weekly` | 可选 | 周排行 |
| GET | `/leaderboard/monthly` | 可选 | 月排行 |
| GET | `/leaderboard/study` | 可选 | 学习时长排行 |
| GET | `/leaderboard/streak` | 可选 | 签到排行 |
| GET | `/leaderboard/my-rank` | 需要 | 我的排名 |

#### 通知
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/notifications` | 需要 | 通知列表（分页） |
| GET | `/notifications/unread-count` | 需要 | 未读数 |
| PUT | `/notifications/:id/read` | 需要 | 标记已读 |
| PUT | `/notifications/read-all` | 需要 | 全部已读 |
| POST | `/notifications/create` | 服务间 | 内部创建通知 |

#### 好友
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/friends/requests` | 需要 | 发送申请 |
| PUT | `/friends/requests/:id/accept` | 需要 | 接受 |
| PUT | `/friends/requests/:id/reject` | 需要 | 拒绝 |
| GET | `/friends/requests` | 需要 | 申请列表 (?type=incoming|outgoing) |
| GET | `/friends/requests/unread-count` | 需要 | 未读申请数 |
| GET | `/friends/search` | 需要 | 搜索用户 |
| GET | `/friends` | 需要 | 好友列表 |
| DELETE | `/friends/:userId` | 需要 | 删除好友 |
| GET | `/friends/status/:targetUserId` | 需要 | 关系状态 |
| POST | `/friends/block` | 需要 | 拉黑 |
| DELETE | `/friends/block/:userId` | 需要 | 取消拉黑 |
| GET | `/friends/blocked` | 需要 | 黑名单 |

#### 关注
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/follows/:userId` | 需要 | 关注/取关 toggle |
| GET | `/follows/:userId/check` | 需要 | 是否关注 |
| GET | `/follows/:userId/followers` | 可选 | 粉丝列表 |
| GET | `/follows/:userId/following` | 可选 | 关注列表 |

#### 管理员
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/admin/users` | admin+ | 用户列表 |
| GET | `/admin/users/:id` | admin+ | 用户详情 |
| PUT | `/admin/users/:id/role` | super_admin | 变更角色 |
| PUT | `/admin/users/:id/status` | admin+ | 禁言/解禁 |
| GET | `/admin/stats` | super_admin | 全局统计 |
| GET | `/admin/stats/trend` | super_admin | 统计趋势 |
| POST | `/admin/apply` | 需要 | 申请管理员 |
| GET | `/admin/applications` | super_admin | 申请列表 |
| PUT | `/admin/applications/:id` | super_admin | 审核申请 |

#### 反馈
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/feedback` | 需要 | 提交反馈 |
| GET | `/feedback` | super_admin | 反馈列表 |
| PUT | `/feedback/:id` | super_admin | 更新反馈状态 |

### 学习内容服务 `/api/study` (3002)

#### 自习室
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/rooms` | 可选 | 自习室列表 |
| GET | `/rooms/:id` | 可选 | 详情 |
| GET | `/rooms/:id/participants` | 可选 | 参与者 |
| POST | `/rooms` | admin+ | 创建 |
| PUT | `/rooms/:id` | admin+ | 编辑 |
| POST | `/rooms/:id/join` | 需要 | 加入 |
| POST | `/rooms/:id/leave` | 需要 | 离开 |

#### 学习记录
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/sessions/start` | 需要 | 开始学习 |
| PUT | `/sessions/:id/end` | 需要 | 结束学习 |
| DELETE | `/sessions/active` | 需要 | 放弃当前 |
| GET | `/sessions/my` | 需要 | 我的记录 |
| GET | `/sessions/active` | 需要 | 当前活跃（计时器恢复） |

#### 实体占座
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/locations` | 可选 | 地点列表 |
| GET | `/locations/:id` | 可选 | 地点详情 |
| GET | `/locations/:id/seats` | 可选 | 座位图 |
| POST | `/locations` | admin+ | 添加地点 |
| PUT | `/locations/:id` | admin+ | 编辑 |
| DELETE | `/locations/:id` | admin+ | 删除 |
| POST | `/locations/:id/seats` | admin+ | 批量添加座位 |
| POST | `/seats/:id/reserve` | 需要 | 预约（检查禁预约状态） |
| PUT | `/reservations/:id/checkin` | 需要 | 签到 |
| PUT | `/reservations/:id/cancel` | 需要 | 取消 |
| GET | `/reservations/my` | 需要 | 我的预约 |
| GET | `/reservations/my/penalty` | 需要 | 违约状态 |

#### 志愿任务
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/volunteer/tasks` | 需要 | 任务列表 |
| POST | `/volunteer/apply/:task_id` | 需要 | 申请 |
| GET | `/admin/volunteer/pending` | admin+ | 待确认 |
| PUT | `/admin/volunteer/:id/confirm` | admin+ | 确认（扣违约+奖积分） |
| PUT | `/admin/volunteer/:id/reject` | admin+ | 拒绝 |

#### 书籍
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/books` | 可选 | 列表/搜索 |
| GET | `/books/:id` | 可选 | 详情 |
| GET | `/books/recommend` | 可选 | 个性化推荐 |
| POST | `/books/submit` | 需要 | 用户推送（待审核） |
| POST | `/books` | admin+ | 直接添加 |
| PUT | `/books/:id` | admin+ | 编辑 |
| DELETE | `/books/:id` | admin+ | 删除 |
| GET | `/admin/books/pending` | admin+ | 待审核 |
| PUT | `/admin/books/:id/review` | admin+ | 审核 |
| POST | `/books/:id/rate` | 需要 | 评分/书评 |
| POST | `/books/:id/collect` | 需要 | 收藏 toggle |
| GET | `/books/collections` | 需要 | 我的收藏 |

#### 自习室聊天
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/chat/rooms/:id/messages` | 需要 | 聊天记录 |
| GET | `/chat/rooms` | 需要 | 聊天房间列表 |
| GET | `/chat/my-rooms` | 需要 | 我加入的房间 |
| POST | `/chat/mark-read` | 需要 | 标记已读 |
| POST | `/chat/upload` | 需要 | 上传聊天图片（10MB, 前端压缩 800×800） |

#### 私聊
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/private/conversations` | 需要 | 会话列表 |
| POST | `/private/conversations` | 需要 | 创建/获取会话 |
| GET | `/private/conversations/:id/messages` | 需要 | 消息记录 |
| PUT | `/private/conversations/:id/read` | 需要 | 标记已读 |
| GET | `/private/unread-count` | 需要 | 未读总数 |
| GET | `/private/users/:userId/status` | 需要 | 在线状态 |
| GET | `/private/online-users` | 需要 | 在线用户列表 |

### 社区服务 `/api/community` (3003)

#### 帖子
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/posts` | 可选 | 帖子列表 |
| GET | `/posts/:id` | 可选 | 详情 |
| POST | `/posts` | 需要 | 创建（直接发布 +5 积分 + @提及通知） |
| PUT | `/posts/:id` | 需要 | 编辑（自动版本记录） |
| DELETE | `/posts/:id` | 需要 | 删除 |
| POST | `/posts/:id/like` | 需要 | 点赞 toggle |
| GET | `/posts/:id/versions` | 可选 | 版本历史 |
| GET | `/posts/:id/versions/:version` | 可选 | 特定版本 |
| POST | `/posts/:id/versions/:version/rollback` | 需要 | 回滚版本 |
| GET | `/my/posts` | 需要 | 我的帖子 |
| GET | `/users/:userId/posts` | 无 | 某用户的帖子 |

#### 评论
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/posts/:postId/comments` | 可选 | 评论列表（含 @提及解析） |
| POST | `/posts/:postId/comments` | 需要 | 评论（+2 积分 + @提及通知） |
| DELETE | `/comments/:id` | 需要 | 删除 |
| POST | `/comments/:id/like` | 需要 | 点赞 toggle |
| GET | `/my/comments` | 需要 | 我的评论 |

#### 标签
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/tags` | 无 | 标签列表 |

#### 收藏与提案
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/bookmarks/:postId` | 需要 | 收藏 toggle |
| GET | `/my/bookmarks` | 需要 | 我的收藏 |
| GET | `/proposals` | 可选 | 提案列表 |
| GET | `/proposals/:id` | 可选 | 提案详情 |
| POST | `/proposals` | 需要 | 创建提案 |
| PUT | `/proposals/:id/merge` | 需要 | 合并提案 |
| PUT | `/proposals/:id/reject` | 需要 | 拒绝提案 |
| DELETE | `/proposals/:id` | 需要 | 关闭提案 |
| GET | `/my/proposals` | 需要 | 我的提案 |

#### 上传
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/upload/image` | 需要 | 文章图片（5MB 限制） |

#### 管理员
| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/admin/posts/pending` | admin+ | 待审核帖子 |
| GET | `/admin/posts/all` | admin+ | 所有帖子 |
| PUT | `/admin/posts/:id/review` | admin+ | 审核 |
| PUT | `/admin/posts/:id/pin` | admin+ | 置顶 toggle |
| PUT | `/admin/posts/:id/feature` | admin+ | 精选 toggle |
| PUT | `/admin/posts/:id/hide` | admin+ | 隐藏 toggle |
| PUT | `/admin/posts/:id/edit` | admin+ | 管理员编辑 |
| DELETE | `/admin/posts/:id` | admin+ | 删除帖子 |
| GET | `/admin/comments/pending` | admin+ | 待审核评论 |
| GET | `/admin/comments/all` | admin+ | 所有评论 |
| PUT | `/admin/comments/:id/review` | admin+ | 审核评论 |
| DELETE | `/admin/comments/:id` | admin+ | 删除评论 |
| GET | `/admin/proposals/pending` | admin+ | 待审核提案 |
| PUT | `/admin/proposals/:id/merge` | admin+ | 合并提案 |
| PUT | `/admin/proposals/:id/reject` | admin+ | 拒绝提案 |
| GET | `/admin/review-logs` | admin+ | 审核日志 |

---

## Socket.IO 事件

Socket.IO 服务端运行在 study-service，客户端通过 Nginx `/socket.io/` 代理连接（WebSocket + polling，最多 10 次重连）。

### 客户端 → 服务端

| 事件 | 数据 | 说明 |
|------|------|------|
| `room:join` | roomId | 加入自习室频道 |
| `room:leave` | roomId | 离开 |
| `seat:join` | locationId | 订阅座位更新 |
| `seat:leave` | locationId | 取消订阅 |
| `chat:message` | { roomId, content, anonymous?, imageUrl? } | 聊天消息（≤2000 字，2s 冷却） |
| `chat:typing` | roomId | 正在输入 |
| `chat:stopTyping` | roomId | 停止输入 |
| `chat:markRead` | { roomId } | 标记已读 |
| `status:update` | { roomId, status, elapsedSeconds } | 更新学习状态 |
| `pm:send` | { toUserId, content, imageUrl? } | 私聊（仅好友，2s 冷却） |
| `pm:typing` | { toUserId } | 私聊输入指示 |
| `pm:stopTyping` | { toUserId } | 停止输入 |
| `pm:markRead` | { conversationId } | 标记私聊已读 |
| `online:heartbeat` | — | 30s 心跳（维持在线状态） |
| `online:getUsers` | callback | 获取在线用户列表 |

### 服务端 → 客户端

| 事件 | 数据 | 说明 |
|------|------|------|
| `chat:message` | { id, roomId, userId, username, avatarUrl, content, imageUrl, type, createdAt } | 新消息 |
| `chat:error` | { message } | 错误 |
| `chat:rateLimited` | { cooldown } | 发送过快 |
| `chat:typing` | { userId, username } | 他人正在输入 |
| `chat:stopTyping` | { userId } | 停止输入 |
| `chat:readUpdate` | { roomId, readByUserId } | 已读回执 |
| `room:onlineUsers` | { roomId, users, count } | 在线用户列表 |
| `room:participants` | (data) | 参与者变化 |
| `status:update` | { userId, username, status, elapsedSeconds } | 学习状态变化 |
| `seat:update` | (data) | 座位状态变化 |
| `pm:message` | { id, conversationId, senderId, senderName, senderAvatar, content, imageUrl, createdAt } | 私聊消息 |
| `pm:error` | { message } | 私聊错误（非好友、被拉黑） |
| `pm:typingStatus` | { userId } | 对方正在输入 |
| `pm:stopTypingStatus` | { userId } | 停止输入 |
| `pm:readUpdate` | { conversationId, readByUserId } | 私聊已读 |
| `online:statusChange` | { userId, username, avatarUrl, status } | 上线/下线（全局广播） |
| `friend:request` | { requestId, fromUserId, fromNickname, fromAvatar } | 新好友申请（Redis Pub/Sub 跨服务） |
| `friend:accepted` | { acceptedByUserId, conversationId } | 好友申请被接受 |

### 跨服务通信

- user-service 通过 Redis Pub/Sub（频道 `xueqi:friend_events`）发布好友事件
- study-service 订阅后通过 Socket.IO 推送给对应用户
- Socket 认证: JWT token 通过 `socket.handshake.auth.token` 传入，心跳时重新验证

---

## 前端架构

### 路由（18 条）

| 路径 | 组件 | 需登录 | 管理员 |
|------|------|--------|--------|
| `/` | HomePage | 否 | — |
| `/login` | LoginPage | 否 | — |
| `/register` | RegisterPage | 否 | — |
| `/study-rooms` | StudyRoomsPage | 否 | — |
| `/seat-booking` | SeatBookingPage | 否 | — |
| `/community` | CommunityPage | 否 | — |
| `/community/create` | KnowledgeEditPage | 是 | — |
| `/community/posts/:id` | PostDetailPage | 否 | — |
| `/community/posts/:id/edit` | KnowledgeEditPage | 是 | — |
| `/community/proposals/create/:postId` | ProposalPage | 是 | — |
| `/community/proposals/:id` | ProposalPage | 否 | — |
| `/messages` | MessagesPage | 是 | — |
| `/messages/:id` | ChatPage | 是 | — |
| `/leaderboard` | LeaderboardPage | 否 | — |
| `/profile` | ProfilePage | 是 | — |
| `/admin` | AdminPage | 是 | 是 |
| `/user/:id` | UserPage | 否 | — |
| `/:pathMatch(.*)*` | NotFoundPage | 否 | — |

**导航守卫**: `beforeEach` 从 localStorage 恢复会话，检查 `requiresAuth` / `requiresAdmin` meta 字段，未登录跳转 `/login?redirect=原路径`

### Pinia Store（4 个）

| Store | 关键状态 | 关键 Actions | Socket 事件 |
|-------|---------|-------------|------------|
| `user` | token, user, unreadCount, isLoggedIn, isAdmin, isSuperAdmin | login, register, logout, fetchProfile, fetchUnreadCount | — |
| `chat` | chatRoomId, roomInfo, isOpen, unreadCount, onlineUsers | openChat, closeChat, toggleChat, fetchRoomList | room:join, room:leave |
| `friend` | friends, incomingRequests, outgoingRequests, unreadRequestCount | fetchFriends, sendRequest, acceptRequest, deleteFriend, blockUser | friend:request, friend:accepted |
| `message` | conversations, currentMessages, unreadTotal, onlineUsers | fetchConversations, openConversation, fetchMessages, markAsRead | pm:message, online:statusChange |

### 关键功能实现

#### 图片上传管线

```
前端 Canvas 压缩 → FormData multipart/form-data → Multer diskStorage → 共享 Docker 卷 → Nginx 静态服务

压缩策略:
- 头像: 300×300, 质量 0.85
- 聊天图片: 800×800, 质量 0.8
- 文章图片: 原图 (Multer 5MB 限制)
- Nginx 兜底: client_max_body_size 15MB
```

#### @提及功能

```
输入 @ → 立即显示好友列表 → 本地过滤 → 可选搜索 API
      → 选择后插入 @username
      → 发帖/评论时后端 mentionService 解析:
         parseMentions(content)     — 正则 /@([\w\u4e00-\u9fff]+)/g
         resolveMentions(usernames) — 查询 users 表
         notifyMentions(...)        — 调用 user-service 创建通知
      → 被提及用户收到通知（可点击跳转到对应帖子/评论锚点）
      → MarkdownViewer 渲染 @username 为可点击链接
      → PostDetailPage 评论渲染为 /user/{userId} 链接
```

#### Markdown 编辑器增强

- 图片上传按钮 + 拖拽 + 粘贴上传
- 前端压缩后上传，插入 `![desc](url)` 占位
- @提及自动补全（输入 @ 触发好友列表）
- 表格插入按钮

#### Markdown 渲染

- marked + DOMPurify 自定义渲染器
- 图片包裹在 `.img-wrap` 中，支持拖拽边界调整大小
- @提及渲染为 `<a>` 链接
- 安全过滤: DOMPurify ADD_TAGS=['span'], ADD_ATTR=['class']

---

## 积分与等级系统

### 积分规则

| 行为 | 积分 | 上限 |
|------|------|------|
| 完成自习（≥25分钟） | +10 | 每日 50 |
| 番茄钟 | +5 | 每日 25 |
| 预约座位并签到 | +15 | 每次 |
| 书籍评分 | +3 | 每书 1 次 |
| 写书评（≥30字） | +5 | 每书 1 次 |
| 推荐书籍被采纳 | +20 | 每次 |
| 发帖 | +5 | 每日 20 |
| 被点赞 | +1 | 无上限 |
| 评论 | +2 | 每日 10 |
| 连续签到 | +n | 上限 +30 |
| 志愿任务 | +5~10 | 每次，同时扣减 1 次违约 |

### 等级体系

| 等级 | 名称 | 积分 | 印章样式 |
|------|------|------|---------|
| Lv.1 | 书童 | 0 | 绿色小印 |
| Lv.2 | 秀才 | 100 | 蓝色方印 |
| Lv.3 | 举人 | 500 | 朱砂方印 |
| Lv.4 | 进士 | 1500 | 金色方印 |
| Lv.5 | 翰林 | 4000 | 金色圆印 |
| Lv.6 | 大儒 | 10000 | 朱砂大印 |

### 违约惩罚机制

```
预约超时 20 分钟未签到
  → 自动释放座位 (status='no_show')
  → penalty_count + 1
  → 禁预约天数 = 违约次数 × 2 天
  → 每月 1 日重置 penalty_count

解除途径:
  完成志愿任务 → 管理员确认 → 扣减 1 次违约 + 奖励积分
```

---

## 视觉设计系统

### 设计理念

**「墨韵书声，栖心之所」** — 传统水墨美学为底色，融合毛玻璃现代效果。

### 色彩方案

| 用途 | 色值 | 名称 | 说明 |
|------|------|------|------|
| 主背景 | `#F5F0E8` | 宣纸白 | 米白底色 |
| 次背景 | `#EDE6D6` | 古卷黄 | 卡片底色 |
| 主文字 | `#2C2C2C` | 浓墨 | 近纯黑 |
| 次文字 | `#6B6B6B` | 淡墨 | 辅助文字 |
| **强调色** | `#8B2500` | **朱砂红** | 按钮、链接、印章 |
| 辅助色1 | `#2E5C4C` | 松绿 | 自习室、学习模块 |
| 辅助色2 | `#4A6B8A` | 靛蓝 | 书籍、知识模块 |
| 辅助色3 | `#B8860B` | 赭金 | 积分、排行榜 |
| 边框色 | `#C4B99A` | 旧绢 | 卡片边框 |

### 字体

| 用途 | 字体 |
|------|------|
| 标题 | `"楷体", "STKaiti", "KaiTi", serif` |
| 正文 | `"思源宋体", "Noto Serif SC", "宋体", serif` |
| 代码 | `"Fira Code", monospace` |

### 毛玻璃策略

- **使用 `backdrop-filter: blur(12px)`**: 导航栏、Hero 区、弹窗（少量固定元素）
- **不使用**: 帖子列表、书籍卡片等大量重复元素（改用 `rgba(245,240,232,0.85)` 半透明背景）

### 装饰元素

| 元素 | 实现方式 |
|------|---------|
| 宣纸纹理 | SVG background-image, opacity 0.5 |
| 印章徽章 | CSS border + transform: rotate(-2deg), 按等级变色 |
| 水墨角饰 | SVG, opacity 0.06 |
| 竖排文字 | `writing-mode: vertical-rl` |
| 涟漪动画 | radial-gradient + CSS @keyframes |
| 卡片墨迹悬浮 | ink-dots.svg, hover 时 opacity 0.06 |

### 页面主题色

| 主题 | 顶部渐变条 | 标题色 | 应用页面 |
|------|-----------|--------|---------|
| community | 朱砂红 → 赭金 | 朱砂红 | 知识广场 |
| study | 松绿 | 松绿 | 书院 |
| books | 靛蓝 | 靛蓝 | 书海 |
| leaderboard | 赭金 | 赭金 | 金榜 |
| profile | 松绿 → 朱砂红 | 松绿 | 个人中心 |
| seat | 朱砂红 → 松绿 | 朱砂红 | 占座 |
| post-detail | 右上角淡红径向渐变 | — | 帖子详情 |
| edit | 朱砂红 | 朱砂红 | 编辑帖子 |
| proposal | 靛蓝 → 朱砂红 | 靛蓝 | 编辑提案 |

### Element Plus 主题覆写

```css
:root {
  --el-color-primary: #8B2500;          /* 朱砂红 */
  --el-color-primary-light-3: #a84d33;
  --el-color-primary-light-5: #c47a66;
  --el-color-primary-light-7: #d9a08f;
  --el-color-primary-light-9: #f0cfc6;
  --el-color-primary-dark-2: #6f1e00;
  --el-border-radius-base: 8px;
  --el-font-family: var(--font-body);
  --el-bg-color: var(--glass-bg-card);
}
```

---

## 部署指南

### Docker Compose 容器（7 个）

| 容器 | 镜像 | 主机端口 | 依赖 |
|------|------|---------|------|
| `xueqi-mysql` | mysql:8.0 | 3307:3306 | — |
| `xueqi-redis` | redis:5.0 | 6380:6379 | — |
| `xueqi-user-service` | custom | 3001:3001 | mysql(healthy), redis(healthy) |
| `xueqi-study-service` | custom | 3002:3002 | mysql(healthy), redis(healthy) |
| `xueqi-community-service` | custom | 3003:3003 | mysql(healthy), redis(healthy) |
| `xueqi-gateway` | custom | 3000:3000 | 三个后端服务 |
| `xueqi-frontend` | custom | 80:80 | gateway |

**命名卷**: `mysql-data`（MySQL 持久化）、`redis-data`（Redis AOF）、`upload-data`（上传文件共享）

**网络**: `xueqi-net`（单 bridge 网络）

### 环境变量（.env）

```env
MYSQL_HOST=xueqi-mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=root123
MYSQL_DATABASE=xueqi_db

REDIS_HOST=xueqi-redis
REDIS_PORT=6379

JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

USER_SERVICE_PORT=3001
STUDY_SERVICE_PORT=3002
COMMUNITY_SERVICE_PORT=3003
GATEWAY_PORT=3000

UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=5242880
```

### Nginx 配置

| 路径 | 行为 |
|------|------|
| `/assets/` | 静态文件，30 天缓存，public+immutable |
| `= /index.html` | 不缓存 (no-cache, no-store) |
| `/api/` | 代理到 gateway:3000，限流 30r/s burst 50，30s 超时 |
| `/socket.io/` | 代理到 study-service:3002，WebSocket，24h 超时 |
| `/uploads/` | 静态文件（共享卷 `/usr/share/nginx/uploads`），7 天缓存 |
| `/` | SPA fallback: `try_files $uri $uri/ /index.html` |

其他: gzip 启用（text/css/json/js/xml/svg，≥256B）、charset utf-8、client_max_body_size 15MB

### 常用部署命令

```bash
# 全量启动（首次或数据库变更后）
docker compose up --build -d

# 仅重建前端（前端代码变更）
cd frontend && npm run build
docker build -t web-xueqi-frontend -f frontend/Dockerfile frontend
docker compose up -d xueqi-frontend

# 仅重建某个后端服务（代码变更）
docker compose build xueqi-user-service
docker compose up -d xueqi-user-service

# 查看日志
docker compose logs -f xueqi-gateway

# 重启单个服务
docker compose restart xueqi-frontend
```

### 注意事项

- **后端服务代码未通过 volume 挂载**，代码变更需重新 `docker compose build` + `up`
- **前端构建产物**在 Docker build 阶段生成，Nginx 直接 serve 静态文件
- **上传文件**通过 `upload-data` 命名卷在 user/study/community 三个服务和 nginx 之间共享
- MySQL 初始化脚本仅**首次启动**执行（`docker-entrypoint-initdb.d`），后续需手动执行新增 migration SQL
- MySQL 配置: `utf8mb4` 字符集 + `utf8mb4_unicode_ci` 排序规则 + `mysql_native_password` 认证插件

---

## 关键技术决策

1. **审核策略**: 帖子/评论直接发布，管理员后续管理；书籍推送需审核。每服务管理自己的内容，避免跨服务依赖
2. **跨服务积分**: 业务服务完成操作后调用用户服务积分接口，失败时 Redis 队列补偿
3. **排行榜缓存**: Redis Sorted Set + 每日全量刷新 + 实时增量
4. **GSAP 动画**: 统一 GSAP + ScrollTrigger，首页桌面端 pin 翻页，移动端降级普通滚动
5. **中国风实现**: CSS 变量主题色 + SVG 宣纸纹理 + CSS border-radius 印章
6. **毛玻璃性能**: 仅导航栏/弹窗使用 `backdrop-filter`，大量卡片用半透明背景
7. **Socket.IO 实时通信**: JWT 认证 + 30s 心跳 + 聊天消息持久化 + 打字指示器 + 已读回执
8. **好友系统跨服务**: user-service 管理 MySQL 数据 + Redis SET 缓存 + Pub/Sub 跨服务事件传递
9. **文件上传**: Multer diskStorage + Docker volume 共享 + 前端压缩
10. **违约惩罚**: 20 分钟超时 → 自动释放 + 违约计数 × 2 天禁预约 + 月度重置 + 志愿任务减免
11. **每日积分重置**: `daily_points` + `daily_reset_date` 首次请求时检查
12. **关注系统**: toggle 模式 + `user_follows` 表 + `follower_count`/`following_count` 冗余字段
13. **图片压缩**: Canvas API 前端压缩（无额外依赖），Nginx 15MB 兜底
14. **@提及**: 正则解析 → 用户名查 ID → 服务间通知 → 前端好友列表即时展示
15. **Markdown 安全**: DOMPurify 过滤 + 自定义 marked 渲染器 + 图片拖拽缩放
16. **数据流完整性**: 后端存储 → Redis 事件 → Socket.IO 推送 → 前端 Store → UI 更新，全链路对齐
