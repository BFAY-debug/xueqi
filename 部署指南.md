# 学栖项目部署指南

> 环境：本地 Windows 电脑 → 阿里云 Ubuntu 服务器（Docker 已装）+ 域名已备案
> HTTPS：Let's Encrypt 免费证书（自动续期）

---

## 第一步：上传代码到 GitHub

### 1.1 在 GitHub 创建仓库

1. 登录 [github.com](https://github.com)
2. 右上角 `+` → `New repository`
3. Repository name: `xueqi`
4. 选择 **Public**
5. **不要**勾选 "Add a README file"
6. 点击 `Create repository`

### 1.2 本地初始化并推送

在本地项目目录打开终端：

```bash
cd D:\web

# 初始化 Git（如果还没有）
git init

# 添加远程仓库（把 YOUR_USERNAME 换成你的 GitHub 用户名）
git remote add origin https://github.com/BFAY-debug/xueqi.git

# 添加所有文件并提交
git add .
git commit -m "v3.8.0"

# 推送到 GitHub
git push -u origin main
```

如果弹出登录窗口，输入 GitHub 用户名和 Personal Access Token（不是密码）。
Token 生成：GitHub → Settings → Developer settings → Personal access tokens → Generate new token，勾选 `repo` 权限。

---

## 第二步：服务器拉取代码

### 2.1 SSH 登录服务器

```bash
ssh root@47.xxx.xxx.xxx
```

### 2.2 拉取代码

```bash
cd /root
git clone https://github.com/BFAY-debug/xueqi.git web
cd web
```

> 如果仓库是 public，不需要认证。如果是 private，需要用 token：
> `git clone https://用户名:TOKEN@github.com/YOUR_USERNAME/xueqi.git web`

---

## 第三步：生成配置文件

```bash
cd /root/web

cat > .env << EOF
MYSQL_HOST=xueqi-mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=$(openssl rand -hex 16)
MYSQL_DATABASE=xueqi_db

REDIS_HOST=xueqi-redis
REDIS_PORT=6379
REDIS_PASSWORD=$(openssl rand -hex 16)

CORS_ORIGIN=https://xueqi.help

JWT_SECRET=$(openssl rand -hex 32)
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

USER_SERVICE_PORT=3001
STUDY_SERVICE_PORT=3002
COMMUNITY_SERVICE_PORT=3003
GATEWAY_PORT=3000

UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=5242880
EOF
```
其他 `$()` 会自动生成随机密码。

---

## 第四步：申请 HTTPS 证书

先确保域名 DNS 已指向服务器 IP（在阿里云云解析 DNS 中添加 A 记录）。

```bash
# 安装 certbot
apt update && apt install -y certbot

# 申请证书（先停掉占用 80 端口的服务）
certbot certonly --standalone -d xueqi.help
```

按提示输入邮箱（用于证书过期提醒），同意条款。

成功后证书保存在：
- 证书：`/etc/letsencrypt/live/xueqi.help/fullchain.pem`
- 私钥：`/etc/letsencrypt/live/xueqi.help/privkey.pem`

---

## 第五步：启动

```bash
cd /root/web
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

这条命令会：
- 下载 MySQL、Redis、Node.js、Nginx 镜像
- 编译前端 Vue 项目
- 构建后端服务
- 启动全部 7 个容器（MySQL、Redis、3 个后端服务、API 网关、Nginx）

首次执行需要 **3-5 分钟**，看到类似输出说明成功：

```
✔ Container xueqi-mysql        Started
✔ Container xueqi-redis        Started
✔ Container xueqi-user-service Started
✔ Container xueqi-study-service Started
✔ Container xueqi-community-service Started
✔ Container xueqi-gateway      Started
✔ Container xueqi-frontend     Started
```

确认全部启动：
```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
```

7 个容器全部 `Up` 就对了。

---

## 第六步：开放服务器端口

在阿里云控制台操作：

1. **云服务器 ECS** → **实例** → 点击你的服务器
2. 左侧菜单 **安全组** → 点击安全组名称 → **入方向** → **手动添加**
3. 添加两条规则：

| 协议类型 | 端口范围 | 授权对象  | 描述             |
| -------- | -------- | --------- | ---------------- |
| TCP      | 80       | 0.0.0.0/0 | HTTP（证书续期） |
| TCP      | 443      | 0.0.0.0/0 | HTTPS            |

SSH 的 22 端口通常已经默认开放。

---

## 第七步：设置证书自动续期

```bash
# 添加定时任务
crontab -e
```

添加这一行（每天凌晨 3 点检查续期）：

```
0 3 * * * certbot renew --quiet --deploy-hook "cd /root/web && docker compose -f docker-compose.yml -f docker-compose.prod.yml restart xueqi-frontend"
```

Let's Encrypt 证书有效期 90 天，certbot 会在到期前 30 天自动续期。

---

## 完成！

访问 `https://xueqi.help`，注册账号、登录、发帖测试一下。

---

## 配置文件说明

项目中有几个关键配置文件，各有不同用途：

### .env（环境变量）

存放数据库密码、JWT 密钥、端口号等配置。**不同环境的值不同**：

| 变量 | 本地开发 | 服务器 |
|------|----------|--------|
| `MYSQL_HOST` | `xueqi-mysql`（Docker 容器名） | 同左 |
| `REDIS_PASSWORD` | `redis123` | 强密码 |
| `CORS_ORIGIN` | `http://localhost` | `https://xueqi.help` |
| `JWT_SECRET` | `your-secret-key-change-in-production` | `openssl rand -hex 32` 生成的随机密钥 |
| `JWT_EXPIRES_IN` | `2h`（方便调试） | `15m`（安全） |

`.env` 被 `.gitignore` 排除，**不会上传到 GitHub**，每台机器需要自己配置。

### .env.local（本地覆盖）

```
MYSQL_HOST=localhost    # 本地直连，不走 Docker
REDIS_HOST=localhost
UPLOAD_DIR=./uploads    # 本地路径
```

代码中 `.env.local` 优先级高于 `.env`（后加载覆盖先加载）。**只存在于本地**，用于非 Docker 开发模式。

### .env.example（环境变量模板）

`.env` 的脱敏模板，密码写 `changeme`，不含真实密钥。**会上传到 GitHub**，新开发者克隆项目后复制为 `.env` 再修改。

### .gitignore（Git 忽略规则）

告诉 Git 哪些文件**不要纳入版本管理**：

| 排除项 | 原因 |
|--------|------|
| `.env`、`.env.local` | 含密码密钥，不能公开 |
| `node_modules/` | 依赖包，`npm install` 重新安装即可 |
| `dist/`、`build/` | 构建产物，Docker 构建时生成 |
| `uploads/*` | 用户上传的文件，不进 Git |
| `ssl/`、`certbot/` | SSL 证书，服务器专属 |
| `.vscode/`、`.idea/` | IDE 个人配置 |
| `.claude/` | Claude Code 工作文件 |

### .dockerignore（Docker 忽略规则）

告诉 Docker 构建镜像时**哪些文件不发送给构建上下文**。内容和 `.gitignore` 类似，但额外排除 `.git` 目录（减小构建上下文体积，加速构建）。

---

### 本地 / GitHub / 服务器 文件差异

| 文件/目录 | 本地（你的电脑） | GitHub 仓库 | 服务器 |
|-----------|:---:|:---:|:---:|
| `src/`（源代码） | ✅ | ✅ | ✅ |
| `package.json` | ✅ | ✅ | ✅ |
| `Dockerfile`、`docker-compose*.yml` | ✅ | ✅ | ✅ |
| `.env.example` | ✅ | ✅ | ✅ |
| `.gitignore`、`.dockerignore` | ✅ | ✅ | ✅ |
| `*.md`（文档） | ✅ | ❌ | ❌ |
| `.env` | ✅（本地配置） | ❌ | ✅（生产配置） |
| `.env.local` | ✅ | ❌ | ❌ |
| `node_modules/` | ✅ | ❌ | ❌（镜像内） |
| `dist/`（构建产物） | ❌ | ❌ | ❌（镜像内） |
| `uploads/`（用户文件） | ✅ | ❌ | ✅（Docker 卷） |
| `ssl/`、`certbot/`（证书） | ✅（自签名测试） | ❌ | ✅（Let's Encrypt） |
| `.claude/` | ✅ | ❌ | ❌ |

**核心原则**：
- **源代码**三处一致，通过 `git push` / `git pull` 同步
- **密钥密码**绝不进 Git，每台机器独立配置 `.env`
- **用户数据**（uploads）和 **SSL 证书**只存在于运行环境，不进版本控制

---

## 以后更新代码

```bash
# 1. 本地电脑：修改代码 → 提交 → 推送
cd D:\web
git add .
git commit -m "描述改动内容"
git push

# 2. 服务器上：拉取最新代码 → 重新构建
ssh root@47.xxx.xxx.xxx
cd /root/web
git pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

---

## 常见问题

**页面打不开？**
```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
docker compose -f docker-compose.yml -f docker-compose.prod.yml logs xueqi-frontend
```

**API 返回 503？**
```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml logs xueqi-redis
docker compose -f docker-compose.yml -f docker-compose.prod.yml logs xueqi-user-service
```

**HTTPS 证书错误？**
```bash
certbot certificates           # 查看证书状态
certbot renew --dry-run        # 测试续期是否正常
```

**想先用 IP 测试不绑域名？** 把 `.env` 里 `CORS_ORIGIN` 改为 `http://47.xxx.xxx.xxx`，用 `docker compose up -d --build`（不带 prod overlay，走 HTTP）。

**数据库需要重建？**
```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml down
docker volume rm web_mysql-data
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```
