# 学栖项目部署指南

> 适用版本：v3.6.0+ | 最后更新：2026-04-28

## 快速部署（3 步）

### 1. 上传代码到服务器

```bash
git clone <你的仓库地址> /root/web
cd /root/web
```

### 2. 生成 .env

```bash
cat > .env << EOF
MYSQL_HOST=xueqi-mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=$(openssl rand -hex 16)
MYSQL_DATABASE=xueqi_db

REDIS_HOST=xueqi-redis
REDIS_PORT=6379
REDIS_PASSWORD=$(openssl rand -hex 16)

CORS_ORIGIN=http://$(curl -s ifconfig.me),http://localhost

JWT_SECRET=$(openssl rand -hex 32)
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

USER_SERVICE_PORT=3001
STUDY_SERVICE_PORT=3002
COMMUNITY_SERVICE_PORT=3003
GATEWAY_PORT=3000

UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=5242880

NODE_ENV=production
EOF
```

> `$()` 会自动执行生成随机密钥。`CORS_ORIGIN` 会自动填入服务器 IP。

### 3. 启动

```bash
# 安装 Docker（如果没有）
curl -fsSL https://get.docker.com | sh

# 启动
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

等 2-3 分钟构建完成，访问 `http://服务器IP` 即可。

---

## 常用命令

```bash
# 重新部署
git pull && docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

# 查看状态/日志
docker compose ps
docker compose logs -f

# 进入数据库
docker exec -it xueqi-mysql mysql -uroot -p$(grep MYSQL_ROOT_PASSWORD .env | cut -d= -f2) xueqi_db

# 数据库重置
docker volume rm web_mysql-data && docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

---

## 可选：绑定域名 + HTTPS（Cloudflare）

如果你有域名，推荐套 Cloudflare（免费 HTTPS + WAF + DDoS 防护）：

1. Cloudflare 添加域名 → DNS 添加 A 记录指向服务器 IP（开启橙色云朵）
2. SSL/TLS → 加密模式选 **Flexible**
3. 开启 Always Use HTTPS
4. 把 `.env` 中 `CORS_ORIGIN` 改为 `https://你的域名`

服务器不需要装证书，Cloudflare 负责全部 HTTPS 加密。

## 可选：安全组加固

在云服务商控制台，建议**只开放 22（SSH）和 80（HTTP）**，关闭 3000-3003、3306、6379 等端口。

## 可选：安全验证

- 浏览器 F12 → Application → Cookies 确认 `refreshToken` 标记为 `HttpOnly`
- F12 → Network 检查响应头包含 `X-Content-Type-Options: nosniff`
- 尝试帖子内容输入 `<script>alert(1)</script>`，确认不执行

## 常见问题

**页面空白？** `docker exec xueqi-frontend nginx -t`

**503？** Redis 没启动：`docker compose ps xueqi-redis` 看是否 healthy

**本地开发？** `docker compose up -d --build`（不加 prod 文件即可）
