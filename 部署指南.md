# 学栖项目部署指南

> 环境：本地 Windows 电脑 → 阿里云 Ubuntu 服务器（Docker 已装）+ 域名已备案

---

## 第一步：把代码传到服务器

### 1.1 找到你的服务器公网 IP

登录阿里云控制台 → 云服务器 ECS → 实例列表 → 找到公网 IP（类似 `47.xxx.xxx.xxx`）

### 1.2 在本地 Windows 电脑上传代码

打开 PowerShell 或 CMD，执行：

```bash
scp -r D:\web root@47.xxx.xxx.xxx:/root/web
```

- 把 `47.xxx.xxx.xxx` 替换成你的服务器公网 IP
- 提示 `Are you sure you want to continue connecting?` 输入 `yes`
- 提示输入密码，输入服务器的 root 密码
- 等几分钟传输完成

> 没有 scp？下载 [WinSCP](https://winscp.net/)，图形界面拖拽上传 `D:\web` 到 `/root/web` 即可。

### 1.3 SSH 登录服务器

```bash
ssh root@47.xxx.xxx.xxx
```

输入密码登录后，确认文件到了：

```bash
ls /root/web
```

应该能看到 `docker-compose.yml`、`services/`、`frontend/` 等文件夹。

---

## 第二步：生成配置文件

在服务器上执行（已通过 SSH 登录）：

```bash
cd /root/web

cat > .env << EOF
MYSQL_HOST=xueqi-mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=$(openssl rand -hex 16)
MYSQL_DATABASE=xueqi_db

REDIS_HOST=xueqi-redis
REDIS_PORT=6379
REDIS_PASSWORD=$(openssl rand -hex 16)

CORS_ORIGIN=https://xueqi.help

JWT_SECRET=$(openssl rand -hex 32)
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

USER_SERVICE_PORT=3001
STUDY_SERVICE_PORT=3002
COMMUNITY_SERVICE_PORT=3003
GATEWAY_PORT=3000

UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=5242880

NODE_ENV=production
EOF
```

把 `你的域名.com` 改成实际域名。其他的不用改，`$()` 会自动生成随机密码。

---

## 第三步：启动

```bash
cd /root/web
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

这条命令会自动完成：
- 下载需要的 Docker 镜像（MySQL、Redis、Node.js、Nginx）
- 编译前端 Vue 项目（相当于"打包"，但都是自动的）
- 构建后端服务
- 启动全部 7 个容器

首次执行需要 **3-5 分钟**，看到类似输出说明成功：

```
✔ Container xueqi-mysql        Started
✔ Container xueqi-redis        Started
✔ Container xueqi-user-service Started
✔ Container xueqi-study-service Started
✔ Container xueqi-community-service Started
✔ Container xueqi-gateway      Started
✔ Container xueqi-frontend     Started
```

确认全部启动：
```bash
docker compose ps
```

7 个容器全部 `Up` 就对了。

---

## 第四步：开放服务器端口

在阿里云控制台操作：

1. 进入 **云服务器 ECS** → **实例** → 点击你的服务器
2. 左侧菜单点 **安全组** → 点击安全组名称 → **入方向** → **手动添加**
3. 添加一条规则：

| 协议类型 | 端口范围 | 授权对象  | 描述    |
| -------- | -------- | --------- | ------- |
| TCP      | 80       | 0.0.0.0/0 | Web访问 |

只需要这一条。SSH 的 22 端口通常已经默认开放了。

---

## 第五步：绑定域名

登录 [Cloudflare](https://dash.cloudflare.com)：

1. **添加站点** → 输入你的域名 → 选 Free 计划
2. **DNS** → **添加记录**：
   - 类型：`A`
   - 名称：`@`
   - IPv4 地址：`你的服务器公网IP`
   - 代理状态：**开启橙色云朵**
3. 去**域名注册商**（阿里云域名控制台）把 DNS 服务器改成 Cloudflare 给的两个地址
4. 回到 Cloudflare：
   - **SSL/TLS** → 加密模式选 **Flexible**
   - **SSL/TLS** → Edge Certificates → 开启 **Always Use HTTPS**

等 DNS 生效（几分钟到几小时），然后访问 `https://你的域名.com`。

---

## 完成！

访问你的网站，注册账号、登录、发帖测试一下。

---

## 以后更新代码

本地电脑改完代码后：

```bash
# 1. 本地电脑：重新上传
scp -r D:\web root@47.xxx.xxx.xxx:/root/web

# 2. 服务器上：重新构建启动
ssh root@47.xxx.xxx.xxx
cd /root/web
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

---

## 常见问题

**页面打不开？**
```bash
docker compose ps                 # 看是否全部 Up
docker compose logs xueqi-frontend  # 看前端日志
```

**API 返回 503？**
```bash
docker compose logs xueqi-redis     # Redis 启动了吗
docker compose logs xueqi-user-service  # 后端服务报错了吗
```

**Cloudflare 报 522？** 阿里云安全组没开 80 端口，回到第四步检查

**想先用 IP 测试不绑域名？** 把 `.env` 里 `CORS_ORIGIN` 改为 `http://47.xxx.xxx.xxx`，浏览器直接访问 `http://47.xxx.xxx.xxx`
