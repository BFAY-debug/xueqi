# 学栖项目部署指南（Cloudflare + Docker）

> 适用版本：v3.6.0+ | 最后更新：2026-04-28

## 架构概览

```
用户 → Cloudflare CDN/WAF → 云服务器(:80/:443)
                                  │
                                  ├── Nginx (HTTPS终止 + 安全头 + CSP + HSTS)
                                  │    ├── /api/*       → Gateway(:3000)
                                  │    ├── /socket.io/* → Study Service(:3002) [WebSocket]
                                  │    ├── /uploads/*   → 共享卷静态文件
                                  │    └── /*           → Vue SPA
                                  │
                                  ├── Gateway(:3000) [仅Docker网络可达]
                                  │    ├── /api/user/*       → User Service(:3001)
                                  │    ├── /api/study/*      → Study Service(:3002)
                                  │    └── /api/community/*  → Community Service(:3003)
                                  │
                                  ├── MySQL 8.0(:3306) [仅Docker网络可达]
                                  └── Redis 7(:6379)  [仅Docker网络可达]
```

## 第一步：域名 DNS 解析

1. 登录 [Cloudflare](https://dash.cloudflare.com)，添加你的域名
2. 在 DNS 设置中添加 A 记录：
   - 类型：`A`
   - 名称：`@`（或你的子域名）
   - IPv4 地址：你的云服务器公网 IP
   - 代理状态：**开启橙色云朵**（Proxied）
3. 等待 DNS 生效（通常几分钟）

## 第二步：Cloudflare SSL 设置

1. 进入 **SSL/TLS** → **Overview**
2. 加密模式选择 **Full (strict)**
3. 进入 **SSL/TLS** → **Edge Certificates**
4. 开启 **Always Use HTTPS**
5. 开启 **Automatic HTTPS Rewrites**
6. 开启 **Minimum TLS Version** → TLS 1.2

## 第三步：云服务器安全组

在云服务商控制台配置安全组/防火墙：

| 端口 | 来源 | 说明 |
|------|------|------|
| 22 | 你的 IP | SSH（建议限制来源 IP） |
| 80 | 0.0.0.0/0 | HTTP（Cloudflare 回源） |
| 443 | 0.0.0.0/0 | HTTPS |
| 3000 | **关闭** | Gateway 不对外暴露，走 Nginx 代理 |

**关闭以下端口（不要对外暴露）：**
- 3001, 3002, 3003（后端服务）
- 3306, 3307（MySQL）
- 6379, 6380（Redis）

> 注意：Cloudflare 会通过 80/443 端口回源到你的服务器，其他端口无需开放。

## 第四步：服务器安装 Docker

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER

# 安装 Docker Compose
sudo apt install docker-compose-plugin
```

## 第五步：生成生产密钥

在服务器上运行以下命令，生成强随机密钥：

```bash
# 生成 JWT_SECRET（64字节）
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# 生成 MySQL 密码（32字节）
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 生成 Redis 密码（32字节）
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## 第六步：配置生产环境变量

在服务器上创建 `.env` 文件：

```bash
# 在项目根目录
cat > .env << 'EOF'
# MySQL
MYSQL_HOST=xueqi-mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=<粘贴生成的MySQL密码>
MYSQL_DATABASE=xueqi_db

# Redis
REDIS_HOST=xueqi-redis
REDIS_PORT=6379
REDIS_PASSWORD=<粘贴生成的Redis密码>

# CORS（改为你的域名）
CORS_ORIGIN=https://your-domain.com

# JWT
JWT_SECRET=<粘贴生成的JWT密钥>
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

# Service Ports
USER_SERVICE_PORT=3001
STUDY_SERVICE_PORT=3002
COMMUNITY_SERVICE_PORT=3003
GATEWAY_PORT=3000

# File Upload
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=5242880

# Production
NODE_ENV=production
EOF
```

## 第七步：申请 Let's Encrypt SSL 证书

```bash
# 安装 certbot
sudo apt install certbot

# 申请证书（替换 your-domain.com 为你的域名）
# 注意：先确保 DNS 已指向服务器且 80 端口可访问
sudo certbot certonly --standalone -d your-domain.com

# 证书文件位置：
# /etc/letsencrypt/live/your-domain.com/fullchain.pem
# /etc/letsencrypt/live/your-domain.com/privkey.pem

# 复制到项目目录
mkdir -p certbot/conf
sudo cp -r /etc/letsencrypt/* certbot/conf/
sudo chown -R $USER:$USER certbot/conf
```

## 第八步：修改 nginx 生产配置

编辑 `frontend/nginx.prod.conf`，替换 `$SSL_DOMAIN` 为你的域名：

```bash
# 替换所有 $SSL_DOMAIN 为实际域名
sed -i 's/\$SSL_DOMAIN/your-domain.com/g' frontend/nginx.prod.conf
```

## 第九步：启动服务

```bash
# 构建并启动（生产模式）
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

# 查看日志
docker compose logs -f

# 检查所有容器状态
docker compose ps
```

## 第十步：验证

### 功能验证
1. 访问 `https://your-domain.com` 确认页面正常
2. 注册新用户、登录、发帖、评论
3. 测试 WebSocket：进入自习室发消息

### 安全验证
4. 在浏览器开发者工具 → Application → Cookies 确认 `refreshToken` 标记为 `HttpOnly`、`Secure`、`SameSite=Strict`
5. 在 Network 面板检查响应头，确认包含：
   - `Strict-Transport-Security: max-age=31536000; includeSubDomains`
   - `Content-Security-Policy: default-src 'self'; ...`
   - `X-Content-Type-Options: nosniff`
   - `X-Frame-Options: SAMEORIGIN`
6. 在浏览器地址栏输入 `http://your-domain.com`，确认自动跳转到 `https://`
7. 尝试在帖子内容中输入 `<script>alert(1)</script>`，确认被 DOMPurify 过滤不执行
8. 尝试 SQL 注入（如 `?pageSize=1;DROP TABLE users--`），确认被 parseInt 阻断

### 服务状态验证
```bash
# 检查所有容器运行
docker compose -f docker-compose.yml -f docker-compose.prod.yml ps

# 检查错误日志
docker compose -f docker-compose.yml -f docker-compose.prod.yml logs --tail 20
```

## 证书自动续期

```bash
# 添加定时任务
sudo crontab -e

# 添加以下行（每天凌晨3点检查续期）
0 3 * * * certbot renew --quiet && docker compose -f /path/to/project/docker-compose.yml -f /path/to/project/docker-compose.prod.yml restart xueqi-frontend
```

## Cloudflare 额外安全设置（推荐）

1. **Security** → **WAF** → 开启托管规则集
2. **Security** → **Bots** → 开启 Bot Fight Mode
3. **Security** → **Settings** → Security Level 设为 `Medium`
4. **Speed** → **Optimization** → 开启 Auto Minify (JavaScript, CSS, HTML)
5. **Caching** → **Configuration** → Browser Cache TTL 设为 `Respect Existing Headers`

## 常见问题

### Q: 启动后前端页面空白？
检查 nginx 配置是否正确：`docker exec xueqi-frontend nginx -t`

### Q: API 返回 503 "服务暂时不可用"？
Redis 可能未启动。检查：`docker compose ps xueqi-redis`，确认状态为 healthy。

### Q: Cookie 中的 refreshToken 未设置？
确认 `NODE_ENV=production` 已设置，且通过 HTTPS 访问（Cookie 的 Secure 标志要求 HTTPS）。

### Q: 如何回退到本地开发模式？
```bash
docker compose -f docker-compose.yml up -d --build   # 不加 prod overlay
```

### Q: 如何更新代码后重新部署？
```bash
git pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```
